import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InformacionpreliminarPage } from './informacionpreliminar.page';

describe('InformacionpreliminarPage', () => {
  let component: InformacionpreliminarPage;
  let fixture: ComponentFixture<InformacionpreliminarPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(InformacionpreliminarPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
